var VNode = require("./VNode");
var inherit = require("raptor-util/inherit");

function VText(value) {
    this.bj_(-1 /* no children */);
    this.ay_ = value;
}

VText.prototype = {
    bx_: true,

    as_: 3,

    ar_: function (doc) {
        return doc.createTextNode(this.ay_);
    },

    bd_: function () {
        return new VText(this.ay_);
    }
};

inherit(VText, VNode);

module.exports = VText;